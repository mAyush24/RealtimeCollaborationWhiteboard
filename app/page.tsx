"use client"

export default function Page() {
  return (
    <main style={{ padding: "2rem", textAlign: "center" }}>
      <h1>Deployment Check ✅</h1>
      <p>
        The main collaborative whiteboard lives in <code>client/</code>. Run it locally with <code>npm run dev</code>{" "}
        from the project root.
      </p>
      <p>This placeholder file only exists to satisfy Next.js deployment requirements in this workspace.</p>
    </main>
  )
}
